name: respo-${{ github.repository }}

on:
  workflow_run:
    workflows: ["OP-Master"]
    types: [completed]

jobs:
  modul_ok:
    runs-on: ubuntu-latest

    steps:
      - name: respo empfängt OP Kern-ID
        run: echo "respo: OP Kern-ID empfangen – Modul = ${{ github.repository }}"

      - name: respo OK + VERIFY
        run: echo "respo: OK – VERIFY – Dienst aktiv – Modul = ${{ github.repository }}"

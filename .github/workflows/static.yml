name: respo-${{ github.repository }}

on:
  workflow_run:
    workflows: ["OP-Master"]
    types: [completed]

jobs:
  modul_ok:
    runs-on: ubuntu-latest

    steps:
      - name: respo empfängt ROOT Kern-ID
        run: echo "respo: ROOT Kern-ID empfangen – Modul = ${{ github.repository }}"

      - name: respo OK + VERIFY
        run: echo "respo: OK – VERIFY – Dienst aktiv – Modul = ${{ github.repository }}"

name: iki7uc-Richtung

on:
  workflow_run:
    workflows: ["iki1uc-Sensorik"]
    types: [completed]

jobs:
  richtung:
    runs-on: ubuntu-latest
    steps:
      - name: iki7uc empfängt Kern-ID
        run: echo "iki7uc: Kern-ID empfangen"

      - name: iki7uc OK + VERIFY
        run: echo "iki7uc: OK – VERIFY – Dienst aktiv"

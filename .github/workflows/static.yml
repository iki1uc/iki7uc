name: iki7uc-Richtung

on:
  workflow_run:
    workflows: ["iki1uc-Sensorik"]
    types: [completed]

jobs:
  richtung:
    runs-on: ubuntu-latest
    steps:
      - name: Richtung liefert Vektor an OP
        run: echo "iki7uc → OP: Richtungsvektor übertragen"

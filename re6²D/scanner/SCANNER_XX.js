// SCANNER_XX – PURE VERSION (kein export, kein import)

function SCANNER_XX(molekuel) {
    return {
        roh: molekuel.xx.roh,
        signatur: molekuel.xx.signatur,
        länge: molekuel.xx.signatur.length
    };
}
